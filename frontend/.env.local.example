# Frontend Environment Variables
# Copy this file to .env.local and update values as needed

# Backend API URL (base URL only, /api prefix is added automatically by the frontend)
# The frontend services will append /api to this URL for all API calls
# Example: http://localhost:8000 becomes http://localhost:8000/api/chat, etc.
# Default: http://localhost:8000
NEXT_PUBLIC_API_URL=http://localhost:8000
