Context: Sẽ có nhiều agents trong hệ thống, ví dụ như ChatAgent hay CanvasAgent. Mỗi agent có thể có nhiều sub-agent trong graph. Tương lai sẽ có nhiều agents nữa.

Backend: nên có API để có thể discover các agent này.
Frontend: nên có thể chọn các agent này để chat.