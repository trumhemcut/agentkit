Khi ở canvas mode thì người dùng có thể chọn một phần nhỏ nội dung, và hỏi agent chỉnh sửa một chút, sau đó agent sẽ trả lời đoạn chỉnh sửa mới. Và frontend cần phải stream lại đúng vùng cần chỉnh sửa trong 1 tổng thể đoạn văn đang có trong canvas.

Artifact pannel:
- Khi user chọn text, đoạn text được chọn nên được tô màu để biết đang chọn đoạn text đó
- User có thể nhấn chuột phải và chat với đoạn text chọn đó.