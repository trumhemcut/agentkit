Dựa theo kế hoạch .docs/1-implementation-plans/030-database-persistence-layer-plan.md, đã implement database rồi. Giờ là lúc không cần local storage nữa. Hãy chuyển frontend về dùng database hết nhé. Và dùng zustand cho phần state management lúc cần thiết.

Dùng sonner toast nếu cần thiết

Khoang hãy lo về camel case hay snake_case, hãy giữ nguyên hiện trạng